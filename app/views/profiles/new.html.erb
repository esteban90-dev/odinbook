<div class="row justify-content-center mt-5">
  <div class="col-5 text-center">
    <h2>Create Profile</h2>
  </div>
</div>

<%= render partial: 'form', locals: { model: user_profile_path(current_user), profile: @profile, method: :post } %>

