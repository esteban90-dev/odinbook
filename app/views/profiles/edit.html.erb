<div class="row justify-content-center mt-5">
  <div class="col-5 text-center">
    <h2>Edit Profile</h2>
  </div>
</div>

<%= render partial: 'form', locals: { model: [current_user, @profile], profile: @profile } %>