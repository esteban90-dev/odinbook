<%= render partial: 'shared/posts/form', locals: { model: [current_user, current_user.posts.new], post: current_user.posts.new, redirect: "timeline" } %>

<% @posts.each do |post| %>
  <%= render partial: 'shared/posts/show', locals: { post: post, redirect: "timeline" } %>
<% end %>