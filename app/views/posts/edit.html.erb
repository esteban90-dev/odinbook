<div class="row mt-5 mb-3">
  <div class="col text-center">
    <h2>Edit Post</h2>
  </div>
</div>

<%= render partial: 'shared/posts/form', locals: { model: @post, post: @post, redirect: @redirect } %>
